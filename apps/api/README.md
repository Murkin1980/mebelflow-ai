# API

HTTP/Cloudflare adapter запланирован поверх принятого `packages/lead-admin` core. В Stage 7 проверены сервисные границы лидов, OTP/CAPTCHA, уведомлений, лимитов и восстановления без сети и секретов.
