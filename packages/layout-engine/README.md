# Layout Engine

Pure deterministic движок прямой кухни. Применяет проверенные команды, нормализует позиции, рассчитывает остаток, проверяет ширины модулей, доборы, технику, верхний ряд и антресоли, возвращает понятные предупреждения.

Публичная точка входа: `src/index.ts`.
