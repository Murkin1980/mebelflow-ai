# TECH_SPEC — MebelFlow AI MVP

## 1. Цель

Создать white-label виджет для мебельного лендинга, в котором потенциальный клиент голосом или текстом формирует предварительное ТЗ прямой кухни.

## 2. Scope MVP

### Поддерживается

- одна прямая стена;
- ширина стены 1200–7000 мм;
- высота помещения 2200–4000 мм;
- нижний ряд;
- верхний ряд;
- антресоли;
- техника;
- текст и голос;
- SVG;
- стиль;
- диапазон цены;
- заявка;
- PDF;
- tenant settings.

### Не поддерживается

- Г-образная кухня;
- П-образная кухня;
- остров;
- эркер;
- скосы;
- нестандартные радиусы;
- файлы SketchUp/PRO100;
- раскрой;
- деталировка;
- станки;
- фотореализм.

## 3. Модули кухни MVP

### Нижние

- `base_cabinet_doors`;
- `base_cabinet_drawers`;
- `sink_cabinet`;
- `dishwasher_450`;
- `dishwasher_600`;
- `oven_base`;
- `cooktop_base`;
- `washing_machine`;
- `filler`;
- `open_base`.

### Высокие

- `fridge`;
- `fridge_tall_unit`;
- `oven_tall_unit`;
- `pantry_tall_unit`;
- `utility_tall_unit`.

### Верхние

- `wall_cabinet_doors`;
- `wall_cabinet_lift`;
- `hood_cabinet`;
- `open_wall`;
- `mezzanine`;
- `upper_filler`.

## 4. Стандартные ширины

Настраиваются tenant, значения по умолчанию:

```text
150, 200, 300, 400, 450, 500, 600, 700, 800, 900, 1000
```

Техника:

- ПММ: 450 или 600;
- духовой шкаф: 600;
- холодильник: 600 по умолчанию;
- стиральная машина: 600.

## 5. Высоты по умолчанию

- корпус нижней тумбы: 720;
- ножки/цоколь: 100;
- столешница: 38;
- итог нижнего ряда: 858, округляемая настройкой tenant;
- рекомендуемая рабочая высота: 860–920;
- фартук: 550–650;
- основные верхние шкафы: 720, 800, 900;
- минимальная антресоль: 400;
- потолочный зазор: 20–50.

## 6. Логика антресолей

```text
available = roomHeight
          - lowerTotalHeight
          - apronHeight
          - ceilingGap
```

Если антресоли включены:

```text
mainHeight + mezzanineHeight = available
mezzanineHeight >= tenant.minMezzanineHeight
mainHeight ∈ tenant.allowedUpperHeights
```

Движок выбирает:

1. точное стандартное сочетание;
2. ближайшее сочетание;
3. вариант с увеличенной антресолью;
4. предупреждение, если решение невозможно.

## 7. Голосовой ввод

### Требования

- кнопка записи;
- индикация;
- возможность остановить;
- транскрипт перед применением;
- команда применяется после подтверждения либо автоматически при высокой уверенности;
- пользователь может отменить.

### Поддерживаемые фразы

- «Кухня три метра».
- «Высота потолка два семьдесят».
- «Мойка слева».
- «После мойки посудомойка шестьсот».
- «После неё тумба с тремя ящиками».
- «Перенеси холодильник вправо».
- «Удали последнюю тумбу».
- «Сделай верхние шкафы 800».
- «Добавь антресоли 400».
- «Покажи джапанди номер три».
- «Верни предыдущий вариант».

## 8. Intent Parser

### Вход

```json
{
  "utterance": "после мойки поставь посудомойку 600",
  "locale": "ru-KZ",
  "projectSummary": {
    "wallWidth": 3000,
    "moduleIds": ["sink-1"],
    "remainingWidth": 2200
  }
}
```

### Выход

```json
{
  "commandId": "cmd_uuid",
  "type": "INSERT_AFTER",
  "referenceId": "sink-1",
  "payload": {
    "moduleType": "dishwasher_600",
    "width": 600
  },
  "confidence": 0.97,
  "needsConfirmation": false
}
```

### Низкая уверенность

```json
{
  "type": "CLARIFY",
  "question": "Вы хотите посудомойку шириной 450 или 600 мм?"
}
```

## 9. Layout Engine

### Обязательные проверки

- размер стены известен;
- ширина положительная;
- ширина разрешена;
- модуль существует;
- reference существует;
- сумма модулей не превышает стену;
- техника имеет допустимую ширину;
- положение не конфликтует;
- верхний ряд не превышает доступную высоту;
- антресоли не ниже минимума;
- добор не отрицательный.

### Остаток

```text
remaining = wallWidth - sum(moduleWidths)
```

Если остаток:

- 0–50: технологический зазор;
- 51–149: предложить добор;
- 150+: предложить модуль или несколько вариантов.

## 10. SVG Renderer

### Обязательные элементы

- стена;
- пол;
- нижний ряд;
- столешница;
- фартук;
- верхний ряд;
- антресоли;
- размеры;
- подписи;
- выбранный модуль;
- предупреждения;
- свободный остаток;
- цветовая схема.

### Масштаб

SVG viewBox должен быть независимым от пикселей.

```text
scale = availableCanvasWidth / wallWidth
```

### Mobile

- горизонтальная прокрутка запрещена в стандартном сценарии;
- подписи должны сокращаться;
- детали открываются по tap;
- выбранный блок подсвечивается;
- редактирование — через диалог, не через сложные drag handles.

## 11. Pricing Engine

Поддержать три стратегии tenant:

1. цена за погонный метр;
2. цена за модуль;
3. гибридная формула.

### Результат

```json
{
  "min": 900000,
  "max": 1100000,
  "currency": "KZT",
  "formulaVersion": 1,
  "confidence": "preliminary",
  "breakdown": [
    {"code": "base_modules", "min": 320000, "max": 380000},
    {"code": "upper_modules", "min": 220000, "max": 270000},
    {"code": "facades", "min": 180000, "max": 240000},
    {"code": "hardware", "min": 90000, "max": 140000}
  ]
}
```

## 12. Style Presets

Минимум пять стилей:

- modern_minimal;
- japandi;
- neoclassic;
- warm_modern;
- scandinavian.

Каждый содержит 3–5 палитр.

## 13. Tenant Admin

### Настройки

- бренд;
- логотип;
- основной цвет;
- телефон;
- WhatsApp;
- язык;
- доступные модули;
- стандартные размеры;
- цены;
- стили;
- бесплатные лимиты;
- цена PDF;
- зачёт PDF в заказ;
- срок хранения;
- disclaimer;
- Telegram.

### Лиды

- статус;
- контакт;
- схема;
- ТЗ;
- цена;
- шаг завершения;
- события;
- комментарий;
- связь;
- экспорт PDF.

## 14. PDF

### Содержимое

- бренд;
- данные клиента;
- дата;
- схема;
- размеры;
- список модулей;
- стиль;
- материалы;
- цена;
- предупреждения;
- disclaimer;
- CTA мебельщика.

### Режимы

- free;
- after_contact;
- after_order;
- paid;
- credited_to_order.

## 15. Антиабьюз

- anonymous session cookie;
- IP rate limit;
- device fingerprint только privacy-safe;
- CAPTCHA после порога;
- OTP телефона;
- лимит команд;
- лимит STT;
- лимит проектов;
- tenant monthly budget;
- hard stop;
- no infinite regenerate;
- idempotency.

## 16. Acceptance Criteria MVP

1. Пользователь голосом задаёт стену 3000 мм.
2. Мойка ставится слева.
3. ПММ 600 добавляется после мойки.
4. Тумба с ящиками добавляется после ПММ.
5. Остаток рассчитан корректно.
6. Команда перемещения работает.
7. Undo возвращает прошлое состояние.
8. Верхний ряд создаётся по правилам.
9. Антресоль не ниже 400 мм.
10. Стиль меняет SVG.
11. Цена пересчитывается.
12. Сессия восстанавливается.
13. Контакт создаёт лид.
14. Мебельщик видит ТЗ.
15. PDF выдаётся по tenant policy.
16. На среднем Android сценарий остаётся пригодным.
17. LLM не может создать неизвестную команду.
18. Ошибочная команда не повреждает состояние.

## 17. Interface Design Skill

Для всех клиентских UI-задач обязателен локальный навык:

```text
skills/mebelflow-conversational-interface/SKILL.md
```

Он применяется при:

- проектировании widget;
- реализации voice/text flow;
- отрисовке SVG;
- создании прогресса;
- выборе стиля;
- отображении цены;
- обработке ошибок;
- клиентском подтверждении;
- UI review.

### Дизайн-направление

**«Спокойная мастерская»**

Интерфейс должен ощущаться:

- профессиональным;
- тёплым;
- тактильным;
- понятным;
- спокойным;
- современным;
- не похожим на generic AI dashboard.

### Постоянные зоны widget

1. Progress Header.
2. Conversation Area.
3. Scheme Stage.
4. Context Action Bar.
5. Status Line.

### Обязательные UI-принципы

- одно основное действие на экран;
- голос и текст равноправны;
- транскрипт видим;
- интерпретация команды прозрачна;
- схема обновляется немедленно;
- undo всегда доступен после изменения;
- не более трёх рекомендаций;
- progressive disclosure;
- цена только как диапазон;
- disclaimer до заказа и PDF;
- ошибки сохраняют состояние;
- touch targets минимум 44×44;
- WCAG 2.2 AA;
- reduced motion;
- обязательная проверка 360×800.

### UI Definition of Done

UI-функция не завершена без:

- mobile screenshot;
- desktop screenshot;
- keyboard pass;
- accessibility smoke;
- slow network state;
- error/empty/offline state;
- visual regression;
- review по трём столпам:
  - Frictionless;
  - Quality Craft;
  - Trustworthy.

